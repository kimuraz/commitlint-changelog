# Commitlint Changelog Generator

Yet another changelog generator based on commitlint.

## Installation

```
$ pnpm pack && pnpm install -g $(pwd)/commitlint-changelog-generator-[version].tgz
```

## Usage

```
$ cchg -h
```